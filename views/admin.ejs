<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
    <link rel="icon" href="https://bartokdance.hu/wp-content/uploads/2025/02/unnamed.png" type="image/x-icon">
    <title>Admin felület</title>
    <style>
        .custom-bg {
            background-color: #2d336b;
        }

        .custom-font-color {
            color: #2d336b;
        }
    </style>
</head>
<body style="background-color: #f5f5f5">
<nav class="navbar navbar-expand-lg custom-bg">
    <div class="container-fluid">
        <ul class="navbar-nav me-auto"></ul>
        <div class="me-auto">
            <p class="text-white text-decoration-none" style="font-size:24px; margin-bottom: 2px">ADMIN FELÜLET</p>
        </div>
    </div>
</nav>

<div class="container mt-5">
    <h3 class="text-center custom-font-color">Csoportok szûrése</h3>
    <div class="mb-4">
        <label for="groupSelect" class="form-label">Válassz csoportot:</label>
        <select id="groupSelect" class="form-select">
            <option selected disabled>Válassz...</option>
            <!-- Ide jönnek a csoportok dinamikusan -->
        </select>
    </div>

    <div id="userList" class="table-responsive" style="display: none;">
        <h4 class="custom-font-color">Felhasználók</h4>
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Név</th>
                    <th>E-mail</th>
                    <th>Telefon</th>
                    <th>Mûvelet</th>
                </tr>
            </thead>
            <tbody id="userTableBody">
                <!-- Ide kerülnek a felhasználók -->
            </tbody>
        </table>
    </div>

    <div id="editUserForm" style="display: none;">
        <h4 class="custom-font-color">Felhasználó szerkesztése</h4>
        <form>
            <div class="mb-3">
                <label class="form-label">Név:</label>
                <input type="text" class="form-control" id="editName">
            </div>
            <div class="mb-3">
                <label class="form-label">E-mail:</label>
                <input type="email" class="form-control" id="editEmail">
            </div>
            <div class="mb-3">
                <label class="form-label">Telefon:</label>
                <input type="text" class="form-control" id="editPhone">
            </div>
            <div class="mb-3">
                <label class="form-label">Születési dátum:</label>
                <input type="date" class="form-control" id="editDOB">
            </div>
            <div class="mb-3">
                <label class="form-label">Csoport(ok):</label>
                <select multiple class="form-select" id="editGroups">
                    <!-- Dinamikusan betöltött csoportok -->
                </select>
            </div>
            <button type="submit" class="btn btn-success">Mentés</button>
        </form>
    </div>
</div>

<%- include ('footer') %>
</body>
</html>
